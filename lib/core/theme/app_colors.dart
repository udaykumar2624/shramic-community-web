import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Dark Theme ──────────────────────────────
  static const Color bg = Color(0xFF0A0F0D);
  static const Color surface = Color(0xFF111916);
  static const Color surface2 = Color(0xFF1A2420);
  static const Color border = Color(0xFF1F2E28);

  static const Color textPrimary = Color(0xFFF0F5F2);
  static const Color textMuted = Color(0xFF6B8F7E);

  // ── Light Theme ─────────────────────────────
  static const Color lightBg = Color(0xFFF5FAF7);
  static const Color lightSurface = Color(0xFFFFFFFF);
  static const Color lightSurface2 = Color(0xFFEDF5F0);
  static const Color lightBorder = Color(0xFFD0E8DC);

  static const Color lightTextPrimary = Color(0xFF0A0F0D);
  static const Color lightTextMuted = Color(0xFF4A7A65);

  // ── Shared Accents ───────────────────────────
  static const Color accent = Color(0xFF3DFFA0);
  static const Color accentYellow = Color(0xFFFFCD3D);
  static const Color accentRed = Color(0xFFFF6B6B);
  static const Color accentBlue = Color(0xFF8AB4FF);
}
